# Lulu & Ellie Storage 5 — Go To Camp

Archive for the **Lulu & Ellie Go To Camp** series.

Source archive: ChatGPT Library `/Lulu and Ellie`.

Import rules:
- Preserve original filenames.
- The `(1)` copy of Go To Camp Book 8 is a confirmed byte-for-byte duplicate and is intentionally omitted.
- Oversized files that cannot be committed in the current pass are tracked in `DEFERRED_OVERSIZE.md`.
